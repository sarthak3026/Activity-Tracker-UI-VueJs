#include <bits/stdc++.h>
using namespace std;

int solve(string &a,string &b){
    bool isMatch = false;
    int res = INT_MAX , n = a.size() , m=b.size();
    if(n!=m)return -1;
    if()
}

int main(){

}