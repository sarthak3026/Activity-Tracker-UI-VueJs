#include <bits/stdc++.h>
using namespace std;

int solve(string &a,string &b){
    bool isMatch = false;
    int res = INT_MAX , n = a.size() , m=b.size();
    if(n!=m)return -1;
    if(a==b)return 0;
    int temp = 0;
    for(int i=n-1;i>=0;i--){
        string tempStr = a.substr(i+1,n)+a.substr
    } 
}

int main(){

}